---
trigger: always_on
---

Tech Stack: Use Flutter 3.27+ and Dart.

State Management: Use Riverpod for all state management. No default setState unless it's a simple local animation.

Architecture: Follow Clean Architecture (Presentation, Domain, Data layers).  

UI Framework: Make the UI feel minimal, modern, and highly visual (use cards, shadows, and clear typography). Do not use default boring Material app bars.

Language: All user-facing text, variable names for domain models, and UI labels MUST be in Traditional Chinese (zh-TW).

Execution: Before running flutter pub add, ask for permission. Only generate functional widgets, no placeholder text without my approval.